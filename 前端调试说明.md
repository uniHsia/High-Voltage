# 前端调试步骤说明

## 问题：点击按钮没有反应

### 调试步骤：

#### 1. 检查浏览器控制台（重要！）

**操作步骤**：
1. 打开浏览器（Chrome/Edge）
2. 按 `F12` 打开开发者工具
3. 点击 "Console"（控制台）标签
4. 查看是否有红色错误信息

**常见错误**：
- `ReferenceError: echarts is not defined` → ECharts未加载
- `TypeError: Cannot read property 'addEventListener' of null` → 按钮ID不对
- `404 Not Found` → API路由错误

---

#### 2. 检查网络请求

**操作步骤**：
1. 在开发者工具中点击 "Network"（网络）标签
2. 点击按钮
3. 观察是否发送了请求到 `/api/run-diagnosis/` 或 `/api/run-health/`

**如果没有请求**：
- JavaScript代码未加载或按钮事件未绑定

**如果有请求但失败**：
- 查看响应状态码（500表示服务器错误）
- 查看响应内容

---

#### 3. 手动测试API接口

**测试诊断接口**：
浏览器访问：`http://127.0.0.1:8000/api/run-diagnosis/`

如果返回JSON数据，说明后端正常。
如果报错，说明后端有问题。

---

#### 4. 检查Django服务器日志

**查看终端输出**：
- 启动Django后，终端会显示访问日志
- 点击按钮时查看是否有请求日志
- 如果有错误，终端会显示详细错误信息

---

### 快速测试方法：

创建测试页面验证按钮功能是否正常。

---

## 可能的修复方案：

### 修复1: 确保charts.js正确加载

在 `base.html` 的 `</body>` 标签前添加：
```html
<script>
    console.log('charts.js loaded');
    console.log('runDiagnosisBtn:', document.getElementById('runDiagnosisBtn'));
</script>
```

### 修复2: 检查按钮是否在charts.js加载后渲染

确保 `base.html` 中脚本加载顺序正确：
```html
<script src="{% static 'js/charts.js' %}"></script>
{% block extra_js %}{% endblock %}
</body>
```

### 修复3: 添加调试输出到charts.js

在按钮点击事件中添加：
```javascript
console.log('Button clicked!');
console.log('Sending request to /api/run-diagnosis/');
```

---

## 下一步操作：

请按照以下步骤调试：
1. 打开浏览器开发者工具（F12）
2. 查看Console是否有错误
3. 点击按钮后查看Network是否有请求
4. 将看到的错误信息发给我

我会根据具体错误信息帮你解决！