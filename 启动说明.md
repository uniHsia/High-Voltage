# 高电压设备智能健康管理一体化平台 - 启动说明

## 项目概述

本项目已完成**AI模型与Django网页的整合**，实现了：
- ✅ 智能诊断模型集成（局部放电识别）
- ✅ 健康评估模型集成（HI指数、RUL预测）
- ✅ 网页端动态调用模型
- ✅ 实时数据可视化展示

---

## 启动步骤

### 1. 确保环境配置

确保已安装以下依赖：
```bash
pip install django torch numpy matplotlib scikit-learn reportlab python-docx
```

### 2. 启动Django服务器

进入项目目录：
```bash
cd E:\hsia_file\dsx\csnetwork\compitition
```

启动服务器：
```bash
python manage.py runserver
```

服务器将在 `http://127.0.0.1:8000/` 启动。

### 3. 访问网页

打开浏览器，访问：`http://127.0.0.1:8000/`

---

## 使用流程

### 登录系统

1. 首次访问会进入登录页面
2. 如果没有账号，点击"注册"按钮
3. 注册成功后返回登录页面登录

### 主要功能界面

#### 1. 首页（设备总览与实时监测）
- 显示设备健康概览
- 实时监测告警信息
- 多源数据趋势图

#### 2. 智能诊断（局部放电识别）

**使用步骤**：
1. 进入"智能诊断"页面
2. 点击右上角**"运行诊断模型"按钮**
3. 系统会自动调用AI模型（约10-30秒）
4. 页面将动态更新以下内容：
   - **PRPD图谱**：显示相位-幅值散点图
   - **诊断结果**：放电类型、置信度、严重程度
   - **概率分布**：各放电类型的识别概率
   - **特征重要性**：模型关注的关键特征
   - **相似案例**：历史相似故障记录

**诊断结果示例**：
- 放电类型：金属颗粒 / 悬浮放电 / 沿面放电 / 气隙放电 / 正常
- 置信度：XX%
- 严重程度：正常 / 轻微 / 中等 / 严重

#### 3. 健康评估（HI指数与RUL预测）

**使用步骤**：
1. 进入"健康评估"页面
2. 点击右上角**"运行健康评估模型"按钮**
3. 系统会自动调用AI模型（约20-60秒）
4. 页面将动态更新以下内容：
   - **HI仪表盘**：当前健康指数（0-100）
   - **健康状态**：良好 / 注意 / 警告 / 严重
   - **多指标融合趋势**：局放、温度、电流振动等传感器数据
   - **异常检测得分历史**：设备异常程度变化趋势
   - **RUL预测**：剩余使用寿命预测（天数和月数）
   - **置信区间**：预测的不确定性范围

**健康评估结果示例**：
- HI指数：92.5（状态：良好）
- RUL预测：186天（约6.2个月）
- 模型：Informer

#### 4. 历史追溯（历史数据查询与报告导出）
- 查询历史诊断记录
- 生成PDF/DOCX格式报告
- 导出历史数据

---

## 技术架构

### 后端架构
- **框架**：Django 4.x
- **AI模型**：
  - `main_Diagnosis_1.py`：交叉注意力局部放电诊断
  - `main_predict_2.py`：改进交叉注意力健康评估
- **API接口**：
  - `/api/run-diagnosis/`：智能诊断接口
  - `/api/run-health/`：健康评估接口

### 前端架构
- **可视化**：ECharts 5.x
- **交互**：原生JavaScript + AJAX
- **样式**：自定义CSS

### 数据流
```
用户点击按钮 → Django API调用 → AI模型运行 → JSON数据返回 → ECharts动态渲染
```

---

## 文件结构

```
compitition/
├── dashboard/
│   ├── main_Diagnosis_1.py       # 诊断模型（已集成）
│   ├── main_predict_2.py         # 健康评估模型（已集成）
│   ├── views.py                  # 后端视图（含API接口）
│   ├── urls.py                   # 路由配置
│   ├── templates/
│   │   └── diagnosis.html        # 智能诊断界面
│   │   └ health.html             # 健康评估界面
│   └── ...
├── static/
│   └── js/
│       └── charts.js             # 图表渲染 + API调用逻辑
├── manage.py
└── ...
```

---

## 注意事项

### 模型运行时间
- **智能诊断模型**：首次运行约30秒，后续约10秒
- **健康评估模型**：首次运行约60秒，后续约20秒
- 首次运行会自动训练模型并保存，后续直接加载已训练模型

### 模拟数据说明
- 当前使用模拟数据进行演示
- 模型生成符合高压设备特征的仿真数据
- 无需额外输入真实数据

### 模型文件位置
模型训练后会保存在：
- `cross_attention_pd_model.pth`（诊断模型）
- `improved_cross_attention_health_assessment.pth`（健康评估模型）

---

## 常见问题

### Q1: 模型运行失败？
检查是否安装所有依赖，特别是PyTorch和NumPy。

### Q2: 图表不显示？
确保浏览器支持JavaScript和HTML5，检查控制台是否有错误。

### Q3: 加载时间过长？
首次运行需要训练模型，耐心等待。后续运行会更快。

### Q4: 如何更换设备数据？
修改模型代码中的参数，或提供真实数据集替换模拟数据生成器。

---

## 项目创新点

### 算法创新
- **交叉注意力机制**：多传感器信息融合
- **改进的注意力网络**：时序依赖捕捉 + 全局聚合

### 实体创新
- **一体化平台**：诊断 + 评估 + 监测 + 历史追溯
- **实时可视化**：动态图表渲染
- **智能决策**：置信区间 + 维护建议

---

## 下一步优化建议

1. **性能优化**：
   - 使用异步任务队列（Celery）处理模型调用
   - 添加模型缓存机制

2. **功能扩展**：
   - 支持真实数据上传
   - 添加多设备并发诊断
   - 集成告警推送系统

3. **用户体验**：
   - 添加历史模型运行记录
   - 支持结果对比分析
   - 优化移动端适配

---

## 技术支持

如有问题，请联系开发团队或查看代码注释。

祝使用愉快！🎉